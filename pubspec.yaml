name: amar_proshno
description: A simple Flutter Quiz Application - Amar Proshno

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: 4.6.6

  # Firebase Authentication
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.0

  # Google Sign-In (pinned to the 6.x API which is simpler/stable for this app)
  google_sign_in: ^6.3.0

flutter:
  uses-material-design: true